
            // Metric.find({ gameId: req.params.game_id }, function (err, metrics) {
            //         metrics.forEach(function (metric) {
            //             playerMetric.create({ player:player.id, metric:metric.id});
            //         })
            // })